---
layout: base
---

<header>
  <a class="margin-right: 20px" href="{{ '/' | url }}">{{ metadata.author.name }}</a>
  <nav>
  {% include "navigation.njk" %}
  </nav>
  <div class="hero">
    <h1>{{ title }}</h1>
    {% if description %}
    <h3>{{ description }}</h3>
    {% endif %}
  </div>
</header>

<main>
  {{ content | safe }}
</main>
